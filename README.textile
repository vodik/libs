h1. libs

By Simon Gomizelj

This a compiled collection of code that I reuse a lot. These snippets are rough and only ever completed into a fuctional state. I have a bad habbit of reimplementing things myself for the sake of learning.

h2. libs/error

A error handling framework loosly inspired by glib's GError.

h2. libs/io

A evented IO system based on glib's IOChannel.

h2. libs/rbtree

A red-black tree implmentation based on "this":http://en.literateprograms.org/Red-black_tree_(C)

h2. libs/sbuf

A character buffer wrapper which can dynamically resize.

h2. libs/skipset

A skipset Implementation
